final HOUR = List.generate(12, (index) => (index + 1).toString()).toList();
final MiNUTE =
    List.generate(60, (index) => index.toString().padLeft(2, '0')).toList();
final List<String> AMPM = ["AM", "PM"];
